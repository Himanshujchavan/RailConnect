spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/railconnect
    username: postgres
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
application:
  name: RailConnect
server:
  port: 8080
logging:
  level:
    org.hibernate.SQL: DEBUG
